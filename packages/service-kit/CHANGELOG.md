# @unerp/service-kit

## 0.3.0

### Minor Changes

- 5c46345: Extension contract v0.2: RBAC scope helpers + `RequireScopes` guard, per-app
  secret support, signed event webhooks (`signWebhook`/`verifyWebhook`), semver
  helpers (`satisfiesMinCoreVersion`), and a `CoreClient` with filtered reads,
  batch reads, and scoped writes.
