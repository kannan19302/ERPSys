# README assets

Drop real assets here to replace the placeholders referenced from the root [README.md](../../README.md):

| File                           | Used for                                         |
| :----------------------------- | :----------------------------------------------- |
| `screenshot-dashboard.png`     | Dashboard screenshot                             |
| `screenshot-module-detail.png` | Module list/detail screenshot                    |
| `demo.gif`                     | Short workflow demo (record at ~10-15fps, <10MB) |

Keep images under ~500KB each (optimize with `pnpm dlx @squoosh/cli` or similar) so the README
stays fast to load.
