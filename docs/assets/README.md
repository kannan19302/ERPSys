# README assets

Drop real assets here to replace the placeholders referenced from the root [README.md](../../README.md):

| File                           | Used for                                                                    |
| :----------------------------- | :-------------------------------------------------------------------------- |
| `screenshot-dashboard.svg`     | Dashboard screenshot — currently a placeholder mockup, swap for a real .png |
| `screenshot-module-detail.svg` | Module list/detail screenshot — placeholder, swap for a real .png           |
| `demo.svg`                     | Demo placeholder — replace with a real `demo.gif` (~10-15fps, <10MB)        |

Keep images under ~500KB each (optimize with `pnpm dlx @squoosh/cli` or similar) so the README
stays fast to load.
