{
  "hooks": {
    "UserPromptSubmit": [
      {
        "matcher": "(?i)\\b(new (feature|module|app|page|screen|functionality|capability)|(build|create|implement|add) (a |an |the )?new (feature|module|app|entity|service|integration)|support for)\\b",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"[PM Gate] New feature/functionality detected. Before any implementation, the product-manager agent MUST check .ai/MODULE_REGISTRY.md to confirm this doesn't already exist. Invoke: 'Use product-manager to check if [feature] already exists'\""
          }
        ]
      }
    ]
  }
}
